#include <bits/stdc++.h>
using namespace std;

void sample(int n)
{
    for (int p = n; p > 0; p = p / 2)
    {
        for (int q = 0; q < p; q++)
        {
            // cout << p << " " << q;
        }
    }
}

int main()
{
    sample(1000000000);
    return 0;
}
